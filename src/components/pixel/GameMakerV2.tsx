import { useEffect, useRef, useState } from "react";
import { TH, TW, VIEW_H, VIEW_W, iso, unIso } from "@/components/pixel/world";

type TerrainKey = "grass";
type Tool = "paint" | "erase" | "erasePlatform" | "place" | "eraseObject" | "select" | "move";
type BuildCategory = "terrain" | "objects" | "decorations" | "foundation";
type EditorMode = "edit" | "play";
type Cell = { gx: number; gy: number };
type PlayerEntity = { gx: number; gy: number };
type EdgeMaterial = "soil" | "rock" | "cliff";
type AssetCategory = "nature" | "decoration";
type AssetId = "testTree";
type CollisionDefinition = { enabled: boolean; footprint: Cell[] };
type AssetDefinition = { id: AssetId; name: string; category: AssetCategory; collision: CollisionDefinition };
type PlacedObject = { id: string; assetId: AssetId; gx: number; gy: number };
type WorldData = { gridSize: number; terrain: Record<string, TerrainKey> };
type FoundationSettings = { edgeMaterial: EdgeMaterial; edgeDepth: number };
type PixelChatMapV1 = { version: 1; id: string; name: string; world: WorldData; foundation: FoundationSettings; objects: PlacedObject[] };
type EditorSnapshot = { world: WorldData; objects: PlacedObject[] };
type HistoryState = { past: EditorSnapshot[]; future: EditorSnapshot[] };

const DEFAULT_GRID_SIZE = 14, GRASS_COLOR = "#4f9d2d", DEFAULT_EDGE_DEPTH = 12, MIN_EDGE_DEPTH = 4, MAX_EDGE_DEPTH = 32;
const DEFAULT_WORLD: WorldData = { gridSize: DEFAULT_GRID_SIZE, terrain: {} };
const EMPTY_HISTORY: HistoryState = { past: [], future: [] };
const MAP_STORAGE_KEY = "pixelchat-game-maker-v2-map-v1", DEFAULT_MAP_NAME = "Untitled Map";
const ASSET_LIBRARY: readonly AssetDefinition[] = [{ id: "testTree", name: "Test Tree", category: "nature", collision: { enabled: true, footprint: [{ gx: 0, gy: 0 }] } }];

function cellKey(gx: number, gy: number) { return `${gx},${gy}`; }
function inBounds(gx: number, gy: number, gridSize: number) { return gx >= 0 && gy >= 0 && gx < gridSize && gy < gridSize; }
function getAsset(id: AssetId) { return ASSET_LIBRARY.find((asset) => asset.id === id); }
function getObjectDepth(object: PlacedObject) { return object.gx + object.gy; }
function getPlayerDepth(player: PlayerEntity) { return player.gx + player.gy; }
function isCellBlocked(objects: PlacedObject[], gx: number, gy: number) { return objects.some((object) => { const collision = getAsset(object.assetId)?.collision; return Boolean(collision?.enabled && collision.footprint.some((offset) => object.gx + offset.gx === gx && object.gy + offset.gy === gy)); }); }
function cloneSnapshot(snapshot: EditorSnapshot): EditorSnapshot { return { world: { ...snapshot.world, terrain: { ...snapshot.world.terrain } }, objects: snapshot.objects.map((object) => ({ ...object })) }; }
function cloneMap(map: PixelChatMapV1): PixelChatMapV1 { return { version: 1, id: map.id, name: map.name, world: { ...map.world, terrain: { ...map.world.terrain } }, foundation: { ...map.foundation }, objects: map.objects.map((object) => ({ ...object })) }; }
function createMapId() { return `map-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`; }
function isEdgeMaterial(value: unknown): value is EdgeMaterial { return value === "soil" || value === "rock" || value === "cliff"; }
function isValidMap(data: unknown): data is PixelChatMapV1 { if (!data || typeof data !== "object") return false; const map = data as Partial<PixelChatMapV1>; return map.version === 1 && typeof map.id === "string" && typeof map.name === "string" && Boolean(map.world) && typeof map.world?.gridSize === "number" && Boolean(map.world?.terrain) && typeof map.world.terrain === "object" && Boolean(map.foundation) && isEdgeMaterial(map.foundation?.edgeMaterial) && typeof map.foundation?.edgeDepth === "number" && Array.isArray(map.objects); }
function traceDiamond(ctx: CanvasRenderingContext2D, gx: number, gy: number) { const p = iso(gx, gy); ctx.beginPath(); ctx.moveTo(p.x, p.y); ctx.lineTo(p.x + TW / 2, p.y + TH / 2); ctx.lineTo(p.x, p.y + TH); ctx.lineTo(p.x - TW / 2, p.y + TH / 2); ctx.closePath(); }
function edgePalette(material: EdgeMaterial) { if (material === "rock") return { right: "#5d6670", left: "#77818b" }; if (material === "cliff") return { right: "#55443a", left: "#6d594d" }; return { right: "#74461f", left: "#9a6430" }; }
function drawFoundationFace(ctx: CanvasRenderingContext2D, a: { x: number; y: number }, b: { x: number; y: number }, color: string, depth: number) { ctx.fillStyle = color; ctx.beginPath(); ctx.moveTo(a.x, a.y); ctx.lineTo(b.x, b.y); ctx.lineTo(b.x, b.y + depth); ctx.lineTo(a.x, a.y + depth); ctx.closePath(); ctx.fill(); }
function drawFoundation(ctx: CanvasRenderingContext2D, terrain: Record<string, TerrainKey>, gridSize: number, material: EdgeMaterial, depth: number) { const has = (gx: number, gy: number) => inBounds(gx, gy, gridSize) && Boolean(terrain[cellKey(gx, gy)]); const palette = edgePalette(material); for (let s = 0; s <= (gridSize - 1) * 2; s++) for (let gx = 0; gx < gridSize; gx++) { const gy = s - gx; if (!has(gx, gy)) continue; const p = iso(gx, gy), right = { x: p.x + TW / 2, y: p.y + TH / 2 }, bottom = { x: p.x, y: p.y + TH }, left = { x: p.x - TW / 2, y: p.y + TH / 2 }; if (!has(gx + 1, gy)) drawFoundationFace(ctx, right, bottom, palette.right, depth); if (!has(gx, gy + 1)) drawFoundationFace(ctx, left, bottom, palette.left, depth); } }
function drawTerrainSurface(ctx: CanvasRenderingContext2D, terrain: Record<string, TerrainKey>, gridSize: number) { ctx.fillStyle = GRASS_COLOR; ctx.beginPath(); let any = false; for (let s = 0; s <= (gridSize - 1) * 2; s++) for (let gx = 0; gx < gridSize; gx++) { const gy = s - gx; if (!terrain[cellKey(gx, gy)]) continue; traceDiamond(ctx, gx, gy); any = true; } if (any) ctx.fill(); }
function drawTestTree(ctx: CanvasRenderingContext2D, gx: number, gy: number) { const p = iso(gx, gy), cx = p.x, groundY = p.y + TH / 2; ctx.fillStyle = "#6b3f20"; ctx.fillRect(Math.round(cx - 3), Math.round(groundY - 20), 6, 20); ctx.fillStyle = "#2f7d32"; ctx.beginPath(); ctx.moveTo(cx, groundY - 48); ctx.lineTo(cx + 17, groundY - 20); ctx.lineTo(cx - 17, groundY - 20); ctx.closePath(); ctx.fill(); ctx.fillStyle = "#4f9d2d"; ctx.beginPath(); ctx.moveTo(cx, groundY - 43); ctx.lineTo(cx + 12, groundY - 24); ctx.lineTo(cx - 12, groundY - 24); ctx.closePath(); ctx.fill(); }
function drawAsset(ctx: CanvasRenderingContext2D, object: PlacedObject) { if (getAsset(object.assetId)?.id === "testTree") drawTestTree(ctx, object.gx, object.gy); }
function drawPlayer(ctx: CanvasRenderingContext2D, player: PlayerEntity) { const p = iso(player.gx, player.gy), y = p.y + TH / 2; ctx.fillStyle = "#203a73"; ctx.fillRect(Math.round(p.x - 6), Math.round(y - 18), 12, 14); ctx.fillStyle = "#f0b28a"; ctx.beginPath(); ctx.arc(p.x, y - 24, 7, 0, Math.PI * 2); ctx.fill(); ctx.fillStyle = "#ffd84a"; ctx.fillRect(Math.round(p.x - 5), Math.round(y - 34), 10, 6); }
function drawGridOverlay(ctx: CanvasRenderingContext2D, gridSize: number) { ctx.save(); ctx.strokeStyle = "rgba(185,205,225,.55)"; for (let gy = 0; gy < gridSize; gy++) for (let gx = 0; gx < gridSize; gx++) { traceDiamond(ctx, gx, gy); ctx.stroke(); } ctx.restore(); }
function drawSelection(ctx: CanvasRenderingContext2D, object: PlacedObject | null) { if (!object) return; ctx.save(); ctx.strokeStyle = "#ffd84a"; ctx.lineWidth = 2; traceDiamond(ctx, object.gx, object.gy); ctx.stroke(); ctx.restore(); }
function drawHover(ctx: CanvasRenderingContext2D, hover: Cell | null) { if (!hover) return; ctx.save(); ctx.strokeStyle = "#f7d44a"; traceDiamond(ctx, hover.gx, hover.gy); ctx.stroke(); ctx.restore(); }
function screenToCell(x: number, y: number, gridSize: number): Cell | null { const raw = unIso(x, y), baseGx = Math.floor(raw.gx), baseGy = Math.floor(raw.gy); for (let gy = baseGy - 1; gy <= baseGy + 1; gy++) for (let gx = baseGx - 1; gx <= baseGx + 1; gx++) { if (!inBounds(gx, gy, gridSize)) continue; const p = iso(gx, gy); if (Math.abs(x - p.x) / (TW / 2) + Math.abs(y - (p.y + TH / 2)) / (TH / 2) <= 1) return { gx, gy }; } return null; }

export default function GameMakerV2() {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const [world, setWorld] = useState<WorldData>(DEFAULT_WORLD); const [objects, setObjects] = useState<PlacedObject[]>([]); const [history, setHistory] = useState<HistoryState>(EMPTY_HISTORY); const [mapId, setMapId] = useState(createMapId); const [mapName, setMapName] = useState(DEFAULT_MAP_NAME); const [mapStatus, setMapStatus] = useState("Not saved"); const [tool, setTool] = useState<Tool>("paint"); const [activeCategory, setActiveCategory] = useState<BuildCategory>("terrain"); const [mode, setMode] = useState<EditorMode>("edit"); const [player, setPlayer] = useState<PlayerEntity | null>(null); const [hover, setHover] = useState<Cell | null>(null); const [showGrid, setShowGrid] = useState(false); const [edgeMaterial, setEdgeMaterial] = useState<EdgeMaterial>("soil"); const [edgeDepth, setEdgeDepth] = useState(DEFAULT_EDGE_DEPTH); const [selectedObjectId, setSelectedObjectId] = useState<string | null>(null); const [selectedAssetId, setSelectedAssetId] = useState<AssetId>(ASSET_LIBRARY[0].id);
  const selectedObject = selectedObjectId ? objects.find((object) => object.id === selectedObjectId) || null : null;
  const currentSnapshot = () => cloneSnapshot({ world, objects });
  const applySnapshot = (snapshot: EditorSnapshot) => { const next = cloneSnapshot(snapshot); setWorld(next.world); setObjects(next.objects); setSelectedObjectId(null); };
  const commitChange = (nextWorld: WorldData, nextObjects: PlacedObject[]) => { const previous = currentSnapshot(); setHistory((current) => ({ past: [...current.past, previous], future: [] })); applySnapshot({ world: nextWorld, objects: nextObjects }); setMapStatus("Not saved"); };
  const undo = () => { if (!history.past.length || mode !== "edit") return; const previous = history.past[history.past.length - 1]; setHistory({ past: history.past.slice(0, -1), future: [currentSnapshot(), ...history.future] }); applySnapshot(previous); setMapStatus("Not saved"); };
  const redo = () => { if (!history.future.length || mode !== "edit") return; const next = history.future[0]; setHistory({ past: [...history.past, currentSnapshot()], future: history.future.slice(1) }); applySnapshot(next); setMapStatus("Not saved"); };
  const saveMap = () => { const map = cloneMap({ version: 1, id: mapId, name: mapName.trim() || DEFAULT_MAP_NAME, world, foundation: { edgeMaterial, edgeDepth }, objects }); localStorage.setItem(MAP_STORAGE_KEY, JSON.stringify(map)); setMapId(map.id); setMapName(map.name); setMapStatus("Saved locally"); };
  const loadMapData = (map: PixelChatMapV1) => { const next = cloneMap(map); setMapId(next.id); setMapName(next.name); setWorld(next.world); setObjects(next.objects); setEdgeMaterial(next.foundation.edgeMaterial); setEdgeDepth(Math.max(MIN_EDGE_DEPTH, Math.min(MAX_EDGE_DEPTH, next.foundation.edgeDepth))); setHistory(EMPTY_HISTORY); setSelectedObjectId(null); setHover(null); setTool("paint"); setMode("edit"); setPlayer(null); setMapStatus("Loaded"); };
  const loadSavedMap = () => { const raw = localStorage.getItem(MAP_STORAGE_KEY); if (!raw) { setMapStatus("No saved map found"); return; } try { const parsed: unknown = JSON.parse(raw); if (!isValidMap(parsed)) throw new Error(); loadMapData(parsed); } catch { setMapStatus("Saved map is invalid"); } };
  const enterPlayMode = () => { const first = Object.keys(world.terrain).map((key) => key.split(",").map(Number)).find(([gx, gy]) => inBounds(gx, gy, world.gridSize) && !isCellBlocked(objects, gx, gy)); if (!first) { setMapStatus("Paint free terrain before starting Play Test"); return; } setSelectedObjectId(null); setHover(null); setPlayer((current) => current && world.terrain[cellKey(current.gx, current.gy)] && !isCellBlocked(objects, current.gx, current.gy) ? current : { gx: first[0], gy: first[1] }); setMode("play"); };
  const movePlayer = (dx: number, dy: number) => setPlayer((current) => { if (!current || mode !== "play") return current; const next = { gx: current.gx + dx, gy: current.gy + dy }; if (!world.terrain[cellKey(next.gx, next.gy)]) return current; if (isCellBlocked(objects, next.gx, next.gy)) return current; return next; });
  useEffect(() => { const onKeyDown = (event: KeyboardEvent) => { if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "z") { event.preventDefault(); event.shiftKey ? redo() : undo(); return; } if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "y") { event.preventDefault(); redo(); return; } if (mode !== "play") return; const moves: Record<string, [number, number]> = { ArrowUp: [0,-1], w: [0,-1], W: [0,-1], ArrowDown: [0,1], s: [0,1], S: [0,1], ArrowLeft: [-1,0], a: [-1,0], A: [-1,0], ArrowRight: [1,0], d: [1,0], D: [1,0] }; const move = moves[event.key]; if (move) { event.preventDefault(); movePlayer(move[0], move[1]); } }; window.addEventListener("keydown", onKeyDown); return () => window.removeEventListener("keydown", onKeyDown); }, [history, world, objects, mode, player]);
  function renderWorld(ctx: CanvasRenderingContext2D) { drawFoundation(ctx, world.terrain, world.gridSize, edgeMaterial, edgeDepth); drawTerrainSurface(ctx, world.terrain, world.gridSize); const renderables = objects.map((object) => ({ depth: getObjectDepth(object), gy: object.gy, gx: object.gx, id: object.id, draw: () => drawAsset(ctx, object) })); if (player) renderables.push({ depth: getPlayerDepth(player), gy: player.gy, gx: player.gx, id: "player", draw: () => drawPlayer(ctx, player) }); renderables.sort((a,b) => a.depth-b.depth || a.gy-b.gy || a.gx-b.gx || a.id.localeCompare(b.id)).forEach((item) => item.draw()); if (mode === "edit" && showGrid) drawGridOverlay(ctx, world.gridSize); if (mode === "edit") { drawSelection(ctx, selectedObject); drawHover(ctx, hover); } }
  useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const ctx = canvas.getContext("2d"); if (!ctx) return; ctx.imageSmoothingEnabled = false; ctx.fillStyle = "#15202b"; ctx.fillRect(0,0,VIEW_W,VIEW_H); renderWorld(ctx); }, [world,objects,player,hover,showGrid,edgeMaterial,edgeDepth,selectedObject,mode]);
  const getCell = (event: React.PointerEvent<HTMLCanvasElement>) => { const rect = event.currentTarget.getBoundingClientRect(); return screenToCell(((event.clientX-rect.left)/rect.width)*VIEW_W, ((event.clientY-rect.top)/rect.height)*VIEW_H, world.gridSize); };
  const erasePlatform = (start: Cell) => { const terrain = { ...world.terrain }, visited = new Set<string>(), queue = [start]; while (queue.length) { const cell = queue.pop()!, key = cellKey(cell.gx,cell.gy); if (visited.has(key) || !terrain[key]) continue; visited.add(key); delete terrain[key]; [[1,0],[-1,0],[0,1],[0,-1]].forEach(([dx,dy]) => queue.push({gx:cell.gx+dx,gy:cell.gy+dy})); } if (visited.size) commitChange({ ...world, terrain }, objects.filter((object) => !visited.has(cellKey(object.gx,object.gy)))); };
  const deleteSelectedObject = () => { if (!selectedObjectId) return; commitChange(world,objects.filter((object)=>object.id!==selectedObjectId)); setTool("select"); };
  const handleCell = (cell: Cell) => { if (mode !== "edit") return; const key = cellKey(cell.gx,cell.gy); if (tool === "paint") { if (!world.terrain[key]) commitChange({ ...world, terrain: { ...world.terrain, [key]: "grass" } },objects); return; } if (tool === "erase") { if (world.terrain[key]) { const terrain={...world.terrain}; delete terrain[key]; commitChange({ ...world, terrain },objects.filter((object)=>object.gx!==cell.gx||object.gy!==cell.gy)); } return; } if (tool === "erasePlatform") return erasePlatform(cell); if (tool === "select") { setSelectedObjectId(objects.find((object)=>object.gx===cell.gx&&object.gy===cell.gy)?.id||null); return; } if (tool === "move") { if (!selectedObjectId||!world.terrain[key]||objects.some((object)=>object.id!==selectedObjectId&&object.gx===cell.gx&&object.gy===cell.gy)) return; commitChange(world,objects.map((object)=>object.id===selectedObjectId?{...object,gx:cell.gx,gy:cell.gy}:object)); setTool("select"); return; } if (tool === "place"&&world.terrain[key]&&!objects.some((object)=>object.gx===cell.gx&&object.gy===cell.gy)) { commitChange(world,[...objects,{id:`${Date.now()}-${cell.gx}-${cell.gy}`,assetId:selectedAssetId,gx:cell.gx,gy:cell.gy}]); return; } if (tool === "eraseObject") { const next=objects.filter((object)=>object.gx!==cell.gx||object.gy!==cell.gy); if(next.length!==objects.length) commitChange(world,next); } };
  const clearWorld = () => { if (mode!=="edit"||(!Object.keys(world.terrain).length&&!objects.length)) return; if(window.confirm("Clear the entire world? This can be undone.")){commitChange({...world,terrain:{}},[]);setPlayer(null);setTool("paint");} };
  const buttonStyle=(active=false):React.CSSProperties=>({width:"100%",marginBottom:8,padding:"9px 10px",border:active?"1px solid #ffd84a":"1px solid #33475c",background:active?"#25354a":"#15202b",color:"#e8eef7",borderRadius:8,textAlign:"left",cursor:"pointer"}); const panel:React.CSSProperties={border:"1px solid #33475c",background:"#101820",borderRadius:12,padding:14}; const playButton:React.CSSProperties={padding:"10px 14px",borderRadius:8,border:"1px solid #54c978",background:mode==="play"?"#2f7d32":"#17261d",color:"#e8eef7",fontWeight:800,cursor:"pointer"}; const categoryButtons:Array<[BuildCategory,string]>=[["terrain","🌱 Terrain"],["objects","🌳 Objects"],["decorations","✨ Decorations"],["foundation","🪨 Foundation"]];
  return <main style={{maxWidth:1400,margin:"0 auto",padding:20,color:"#e8eef7"}}><header style={{...panel,marginBottom:14,padding:12,display:"flex",gap:12,alignItems:"center",flexWrap:"wrap"}}><div style={{fontWeight:900,letterSpacing:1}}>PIXELCHAT GAME MAKER V2</div><input value={mapName} disabled={mode==="play"} onChange={(event)=>{setMapName(event.target.value);setMapStatus("Not saved");}} style={{minWidth:180,padding:9,borderRadius:7}}/><span style={{color:"#9fb0c2",fontSize:13}}>{mode==="play"?"PLAY TEST":mapStatus}</span><div style={{marginLeft:"auto",display:"flex",gap:8,flexWrap:"wrap"}}><button disabled={mode==="play"||!history.past.length} onClick={undo}>↶ Undo</button><button disabled={mode==="play"||!history.future.length} onClick={redo}>↷ Redo</button><button disabled={mode==="play"} onClick={saveMap}>Save</button><button disabled={mode==="play"} onClick={loadSavedMap}>Load</button>{mode==="edit"?<button onClick={enterPlayMode} style={playButton}>▶ PLAY TEST</button>:<button onClick={()=>setMode("edit")} style={playButton}>✎ EDIT MODE</button>}</div></header><section style={{display:"grid",gridTemplateColumns:"230px minmax(0,1fr) 250px",gap:14,alignItems:"start"}}><aside style={panel}><div style={{fontSize:12,letterSpacing:1.5,color:"#7f95aa",marginBottom:10}}>BUILD TOOLS</div>{mode==="play"?<div style={{color:"#9fb0c2",lineHeight:1.5}}>Play Test is active.<br/>Use <b>WASD</b> or <b>Arrow Keys</b> to move.</div>:<>{categoryButtons.map(([category,label])=><button key={category} onClick={()=>setActiveCategory(category)} style={buttonStyle(activeCategory===category)}>{label}</button>)}<hr style={{borderColor:"#33475c",margin:"12px 0"}}/>{activeCategory==="terrain"&&<><button onClick={()=>setTool("paint")} style={buttonStyle(tool==="paint")}>Paint Grass</button><button onClick={()=>setTool("erase")} style={buttonStyle(tool==="erase")}>Erase Cell</button><button onClick={()=>setTool("erasePlatform")} style={buttonStyle(tool==="erasePlatform")}>Erase Platform</button></>}{activeCategory==="objects"&&<><div style={{color:"#9fb0c2",fontSize:12,marginBottom:8}}>ASSET LIBRARY</div>{ASSET_LIBRARY.filter((asset)=>asset.category==="nature").map((asset)=><button key={asset.id} onClick={()=>{setSelectedAssetId(asset.id);setTool("place");}} style={buttonStyle(tool==="place"&&selectedAssetId===asset.id)}>🌳 {asset.name}</button>)}<button onClick={()=>setTool("select")} style={buttonStyle(tool==="select")}>Select</button><button onClick={()=>setTool("eraseObject")} style={buttonStyle(tool==="eraseObject")}>Delete Object</button><button disabled={!selectedObject} onClick={()=>setTool("move")} style={buttonStyle(tool==="move")}>Move Selected</button></>}{activeCategory==="decorations"&&<div style={{color:"#9fb0c2",lineHeight:1.5}}>Decoration assets will appear here as the library grows.</div>}{activeCategory==="foundation"&&<><div style={{marginBottom:8}}>Material</div>{(["soil","rock","cliff"] as EdgeMaterial[]).map((material)=><button key={material} onClick={()=>{setEdgeMaterial(material);setMapStatus("Not saved");}} style={buttonStyle(edgeMaterial===material)}>{material.toUpperCase()}</button>)}<label style={{display:"block",color:"#9fb0c2",marginTop:10}}>Depth · {edgeDepth}px<input type="range" min={MIN_EDGE_DEPTH} max={MAX_EDGE_DEPTH} value={edgeDepth} onChange={(event)=>{setEdgeDepth(Number(event.target.value));setMapStatus("Not saved");}} style={{width:"100%"}}/></label></>}</>}</aside><section style={{...panel,padding:10}}><div style={{display:"flex",justifyContent:"space-between",marginBottom:8,color:"#9fb0c2",fontSize:13}}><span>{mode==="play"?"PLAY TEST · Terrain + object collision active":"EDIT MODE"}</span>{mode==="edit"&&<button onClick={()=>setShowGrid((current)=>!current)}>GRID {showGrid?"ON":"OFF"}</button>}</div><canvas ref={canvasRef} width={VIEW_W} height={VIEW_H} onPointerMove={(event)=>mode==="edit"&&setHover(getCell(event))} onPointerLeave={()=>setHover(null)} onPointerDown={(event)=>{const cell=getCell(event);if(cell)handleCell(cell);}} style={{width:"100%",height:"auto",display:"block",imageRendering:"pixelated",cursor:mode==="edit"?"crosshair":"default"}}/></section><aside style={panel}><div style={{fontSize:12,letterSpacing:1.5,color:"#7f95aa",marginBottom:10}}>INSPECTOR</div>{mode==="play"&&<><b>TEST PLAYER</b><div style={{color:"#9fb0c2",marginTop:8}}>Cell: {player?`${player.gx}, ${player.gy}`:"Not placed"}</div><div style={{color:"#9fb0c2",marginTop:8}}>Movement: WASD / Arrow Keys</div><div style={{color:"#9fb0c2",marginTop:8}}>Collision: Terrain + objects</div><div style={{color:"#9fb0c2",marginTop:8}}>Depth: {player?getPlayerDepth(player):"—"}</div></>}{mode==="edit"&&selectedObject&&<><b>{getAsset(selectedObject.assetId)?.name}</b><div style={{color:"#9fb0c2",marginTop:8}}>Cell: {selectedObject.gx}, {selectedObject.gy}</div><div style={{color:"#9fb0c2",marginTop:8}}>Collision: {getAsset(selectedObject.assetId)?.collision.enabled?"Blocking":"Walkable"}</div><div style={{color:"#9fb0c2",marginTop:8}}>Depth: {getObjectDepth(selectedObject)}</div><button onClick={deleteSelectedObject} style={{marginTop:12}}>Delete</button></>}{mode==="edit"&&!selectedObject&&activeCategory==="foundation"&&<><b>FOUNDATION</b><div style={{color:"#9fb0c2",marginTop:8}}>Material: {edgeMaterial}</div><div style={{color:"#9fb0c2",marginTop:8}}>Depth: {edgeDepth}px</div></>}{mode==="edit"&&!selectedObject&&activeCategory!=="foundation"&&<><b>MAP</b><div style={{color:"#9fb0c2",marginTop:8}}>Terrain cells: {Object.keys(world.terrain).length}</div><div style={{color:"#9fb0c2",marginTop:8}}>Objects: {objects.length}</div><div style={{color:"#9fb0c2",marginTop:8}}>ID: {mapId}</div><button onClick={clearWorld} style={{marginTop:18,color:"#ffb0b0"}}>Clear World…</button></>}</aside></section></main>;
}
