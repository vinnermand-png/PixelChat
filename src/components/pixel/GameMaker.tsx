import React, { useEffect, useMemo, useRef, useState } from "react";
import { TH, TW, VIEW_H, VIEW_W, drawTorvetGround, iso, unIso } from "@/components/pixel/world";

type Mode = "empty" | "grass" | "test";
type Tool = "paint" | "fill" | "place" | "select" | "erase";
type Cat = "nature" | "building" | "furniture" | "character" | "effect";
type TerrainKey = string;
type Settings = { grid: number; startMode: Mode; smoothing: boolean; snap: boolean };
type Obj = { id: string; assetId: string; gx: number; gy: number };
type Spec = { key: string; label: string; cat: Cat; prompt: string; w: number; h: number };
type Asset = { id: string; title: string; image: string; spec: Spec };
type Draft = { id: string; v: number; label: string; prompt: string };
type TerrainDef = { key: string; label: string; color: string; edge: string; hint: string; image?: string; ai?: boolean };

const SK = "pixelchat-platform-settings-v5";
const PK = "pixelchat-project-v6";
const LK = "pixelchat-library-v8";
const TK = "pixelchat-foundation-library-v2";
const CK = "pixelchat-ai-cache-v7";
const DEF: Settings = { grid: 14, startMode: "empty", smoothing: false, snap: true };

const BUILTIN_TERRAIN: TerrainDef[] = [
  { key: "grass", label: "GRASS", color: "#4b922d", edge: "#6aaa3d", hint: "Default natural ground" },
  { key: "dirt", label: "DIRT", color: "#a96a2b", edge: "#c17d36", hint: "Paths and bare earth" },
  { key: "stone", label: "STONE GROUND", color: "#59606b", edge: "#737c88", hint: "Rocky floor" },
  { key: "water", label: "WATER", color: "#287fa3", edge: "#43a5c4", hint: "Water surface" },
  { key: "sand", label: "SAND", color: "#c9ad63", edge: "#e2c878", hint: "Beach and desert" },
  { key: "moss", label: "MOSS", color: "#356f3b", edge: "#4b944e", hint: "Dark natural ground" },
  { key: "snow", label: "SNOW", color: "#d7e4ee", edge: "#ffffff", hint: "Winter ground" },
  { key: "forest", label: "FOREST FLOOR", color: "#56452f", edge: "#745f42", hint: "Forest soil and leaves" },
];
const FOUNDATION_PRESETS = [
  ["grass", "GRASS", "A fresh green fantasy grass foundation with subtle natural pixel variation"],
  ["dirt", "DIRT", "A warm brown packed earth foundation with subtle pixel texture"],
  ["water", "WATER", "A clear blue water foundation with small readable pixel ripples"],
  ["stone", "STONE", "A grey stone ground foundation with readable pixel rock variation"],
  ["sand", "SAND", "A warm sandy foundation with subtle pixel grain variation"],
  ["snow", "SNOW", "A clean snow foundation with gentle blue pixel shading"],
  ["moss", "MOSS", "A dark green moss foundation with organic pixel texture"],
  ["custom", "CUSTOM FOUNDATION", "A custom PixelChat foundation terrain tile"],
] as const;
const SPECS: Spec[] = [
  { cat: "nature", key: "tree", label: "TREE", prompt: "A natural forest tree", w: 64, h: 96 },
  { cat: "nature", key: "pine", label: "PINE TREE", prompt: "A tall compact pine tree", w: 56, h: 96 },
  { cat: "nature", key: "bush", label: "BUSH", prompt: "A compact green forest bush", w: 48, h: 40 },
  { cat: "nature", key: "plant", label: "PLANT", prompt: "A small green plant", w: 28, h: 36 },
  { cat: "nature", key: "flower", label: "FLOWER", prompt: "A small wild flower", w: 22, h: 28 },
  { cat: "nature", key: "mushroom", label: "MUSHROOM", prompt: "A small forest mushroom", w: 24, h: 24 },
  { cat: "nature", key: "rock", label: "ROCK", prompt: "A compact natural grey rock", w: 36, h: 28 },
  { cat: "nature", key: "boulder", label: "BOULDER", prompt: "A rounded natural boulder", w: 48, h: 36 },
  { cat: "nature", key: "log", label: "LOG", prompt: "A fallen forest log", w: 48, h: 24 },
  { cat: "nature", key: "stump", label: "TREE STUMP", prompt: "A small cut tree stump", w: 34, h: 30 },
  { cat: "building", key: "house", label: "HOUSE", prompt: "A small cozy game house", w: 96, h: 96 },
  { cat: "building", key: "cabin", label: "CABIN", prompt: "A rustic wooden cabin", w: 96, h: 96 },
  { cat: "furniture", key: "table", label: "TABLE", prompt: "A simple wooden table", w: 44, h: 40 },
  { cat: "furniture", key: "chair", label: "CHAIR", prompt: "A simple wooden chair", w: 32, h: 46 },
  { cat: "character", key: "player", label: "PLAYER", prompt: "A friendly game player", w: 32, h: 48 },
  { cat: "character", key: "npc", label: "NPC", prompt: "A friendly town NPC", w: 32, h: 48 },
  { cat: "effect", key: "fire", label: "FIRE", prompt: "A small magical fire effect", w: 32, h: 44 },
];
const FIRST = SPECS[0];
const panel: React.CSSProperties = { background: "#151f2e", border: "2px solid #3b4c63" };
const heading: React.CSSProperties = { color: "#f2c34d", fontWeight: 900, letterSpacing: 1.2, margin: 0 };

function Button({ kind = "primary", active = false, children, style, ...props }: React.ButtonHTMLAttributes<HTMLButtonElement> & { kind?: "primary" | "nav" | "danger" | "success"; active?: boolean }) {
  const p = { primary: ["#f6c431", "#111827", "#8f6c16"], nav: [active ? "#24384d" : "#172435", active ? "#ffffff" : "#d9e5f0", active ? "#47b9d0" : "#40546d"], danger: ["#8d3d35", "#fff3ef", "#d26b5f"], success: ["#176b61", "#e9fffb", "#45c7b5"] }[kind];
  return <button {...props} style={{ background: p[0], color: p[1], border: `2px solid ${p[2]}`, boxShadow: active ? "0 0 0 1px #f6c431" : "2px 2px #0b1320", padding: "11px 16px", minHeight: 42, fontFamily: "monospace", fontSize: 14, fontWeight: 900, letterSpacing: .4, cursor: "pointer", ...style }}>{children}</button>;
}

const cellKey = (gx: number, gy: number) => `${gx},${gy}`;
function allCells(grid: number, terrain: TerrainKey) { const out: Record<string, TerrainKey> = {}; for (let x = 0; x < grid; x++) for (let y = 0; y < grid; y++) out[cellKey(x, y)] = terrain; return out; }
function loadImage(src: string) { return new Promise<HTMLImageElement>((resolve, reject) => { const i = new Image(); i.onload = () => resolve(i); i.onerror = () => reject(Error("Image load failed")); i.src = src; }); }

async function normalizeObject(src: string, s: Spec) {
  const i = await loadImage(src); const c = document.createElement("canvas"); const x = c.getContext("2d", { willReadFrequently: true }); if (!x) throw Error("Canvas unavailable");
  c.width = i.naturalWidth; c.height = i.naturalHeight; x.drawImage(i, 0, 0); const d = x.getImageData(0, 0, c.width, c.height).data;
  let minX = c.width, minY = c.height, maxX = -1, maxY = -1, edge = 0, solid = 0;
  for (let y = 0; y < c.height; y++) for (let px = 0; px < c.width; px++) { const a = d[(y * c.width + px) * 4 + 3]; if (a > 16) { solid++; if (px < 2 || y < 2 || px > c.width - 3 || y > c.height - 3) edge++; minX = Math.min(minX, px); minY = Math.min(minY, y); maxX = Math.max(maxX, px); maxY = Math.max(maxY, y); } }
  if (maxX < minX) throw Error("Validation failed: asset is empty");
  if (edge / Math.max(1, solid) >= 0.03) throw Error("Validation failed: background must be transparent");
  const cw = maxX - minX + 1, ch = maxY - minY + 1, t = document.createElement("canvas"), z = t.getContext("2d"); if (!z) throw Error("Canvas unavailable");
  t.width = s.w; t.height = s.h; z.imageSmoothingEnabled = false; const scale = Math.min((s.w - 2) / cw, (s.h - 2) / ch), w = Math.max(1, Math.round(cw * scale)), h = Math.max(1, Math.round(ch * scale));
  z.drawImage(c, minX, minY, cw, ch, Math.round((s.w - w) / 2), s.h - h, w, h); return t.toDataURL("image/png");
}

async function normalizeFoundation(src: string) {
  const i = await loadImage(src), t = document.createElement("canvas"), x = t.getContext("2d"); if (!x) throw Error("Canvas unavailable");
  t.width = TW; t.height = TH; x.imageSmoothingEnabled = false; x.drawImage(i, 0, 0, i.naturalWidth, i.naturalHeight, 0, 0, TW, TH); return t.toDataURL("image/png");
}

export default function GameMaker() {
  const ref = useRef<HTMLCanvasElement | null>(null); const imgs = useRef<Record<string, HTMLImageElement>>({}); const terrainImgs = useRef<Record<string, HTMLImageElement>>({});
  const [settings, setSettings] = useState<Settings>(DEF); const [ready, setReady] = useState(false); const [objects, setObjects] = useState<Obj[]>([]); const [library, setLibrary] = useState<Asset[]>([]); const [customTerrain, setCustomTerrain] = useState<TerrainDef[]>([]);
  const [terrain, setTerrain] = useState<Record<string, TerrainKey>>({}); const [tool, setTool] = useState<Tool>("paint"); const [selectedAsset, setSelectedAsset] = useState(""); const [selectedTerrain, setSelectedTerrain] = useState<TerrainKey>("grass"); const [hover, setHover] = useState<{ gx: number; gy: number } | null>(null);
  const [status, setStatus] = useState("READY · CLEAN PROJECT · CHOOSE TERRAIN OR GENERATE AN ASSET"); const [settingsOpen, setSettingsOpen] = useState(false); const [factory, setFactory] = useState(false); const [factoryKind, setFactoryKind] = useState<"object" | "foundation">("object"); const [cat, setCat] = useState<Cat>("nature");
  const [specKey, setSpecKey] = useState(FIRST.key); const [foundationKey, setFoundationKey] = useState("grass"); const [search, setSearch] = useState(""); const [prompt, setPrompt] = useState(FIRST.prompt); const [drafts, setDrafts] = useState<Draft[]>([]); const [pick, setPick] = useState<string | null>(null); const [real, setReal] = useState<string | null>(null); const [generating, setGenerating] = useState(false); const [error, setError] = useState("");
  const spec = SPECS.find(s => s.key === specKey) || FIRST; const allTerrain = useMemo(() => [...BUILTIN_TERRAIN, ...customTerrain], [customTerrain]); const activeTerrain = allTerrain.find(t => t.key === selectedTerrain) || BUILTIN_TERRAIN[0]; const visible = useMemo(() => SPECS.filter(s => s.cat === cat && s.label.toLowerCase().includes(search.toLowerCase())), [cat, search]); const draft = drafts.find(d => d.id === pick) || null;
  const cacheObject = (id: string, src: string) => { const i = new Image(); i.onload = draw; i.src = src; imgs.current[id] = i; }; const cacheTerrain = (id: string, src: string) => { const i = new Image(); i.onload = draw; i.src = src; terrainImgs.current[id] = i; };

  useEffect(() => { try { const ss = JSON.parse(localStorage.getItem(SK) || "null"), sp = JSON.parse(localStorage.getItem(PK) || "null"), sl = JSON.parse(localStorage.getItem(LK) || "[]"), st = JSON.parse(localStorage.getItem(TK) || "[]"); if (ss) setSettings({ ...DEF, ...ss }); if (sp?.objects) setObjects(sp.objects); if (sp?.terrain) setTerrain(sp.terrain); if (Array.isArray(sl)) { setLibrary(sl); sl.forEach((a: Asset) => cacheObject(a.id, a.image)); } if (Array.isArray(st)) { setCustomTerrain(st); st.forEach((t: TerrainDef) => t.image && cacheTerrain(t.key, t.image)); } } catch {} setReady(true); }, []);
  useEffect(() => { if (!ready) return; localStorage.setItem(SK, JSON.stringify(settings)); localStorage.setItem(PK, JSON.stringify({ objects, terrain })); localStorage.setItem(LK, JSON.stringify(library)); localStorage.setItem(TK, JSON.stringify(customTerrain)); }, [settings, objects, terrain, library, customTerrain, ready]);

  function drawTile(x: CanvasRenderingContext2D, gx: number, gy: number, key: TerrainKey | undefined) { const p = iso(gx, gy), t = allTerrain.find(q => q.key === key); x.beginPath(); x.moveTo(p.x, p.y); x.lineTo(p.x + TW / 2, p.y + TH / 2); x.lineTo(p.x, p.y + TH); x.lineTo(p.x - TW / 2, p.y + TH / 2); x.closePath(); if (t?.image && terrainImgs.current[t.key]?.complete) { x.save(); x.clip(); x.imageSmoothingEnabled = false; x.drawImage(terrainImgs.current[t.key], p.x - TW / 2, p.y, TW, TH); x.restore(); } else if (t) { x.fillStyle = t.color; x.fill(); if ((gx + gy) % 3 === 0) { x.fillStyle = t.edge; x.fillRect(Math.round(p.x - 2), Math.round(p.y + TH / 2), 2, 2); } } x.strokeStyle = t ? t.edge : "#26354a"; x.lineWidth = 1; x.stroke(); }
  function draw() { const c = ref.current, x = c?.getContext("2d"); if (!c || !x) return; x.imageSmoothingEnabled = false; x.fillStyle = "#101827"; x.fillRect(0, 0, VIEW_W, VIEW_H); if (settings.startMode === "test" && Object.keys(terrain).length === 0) drawTorvetGround(x); else for (let gx = 0; gx < settings.grid; gx++) for (let gy = 0; gy < settings.grid; gy++) drawTile(x, gx, gy, terrain[cellKey(gx, gy)]); [...objects].sort((a, b) => a.gx + a.gy - b.gx - b.gy).forEach(o => { const a = library.find(q => q.id === o.assetId), i = imgs.current[o.assetId]; if (a && i?.complete) { const p = iso(o.gx, o.gy); x.drawImage(i, p.x - a.spec.w / 2, p.y + TH - a.spec.h, a.spec.w, a.spec.h); } }); if (hover) { const p = iso(hover.gx, hover.gy); x.strokeStyle = "#f6c431"; x.lineWidth = 2; x.beginPath(); x.moveTo(p.x, p.y); x.lineTo(p.x + TW / 2, p.y + TH / 2); x.lineTo(p.x, p.y + TH); x.lineTo(p.x - TW / 2, p.y + TH / 2); x.closePath(); x.stroke(); } }
  useEffect(draw, [objects, library, terrain, customTerrain, settings, hover]);

  function cell(e: React.PointerEvent<HTMLCanvasElement>) { const c = ref.current; if (!c) return null; const r = c.getBoundingClientRect(), u = unIso((e.clientX - r.left) / r.width * VIEW_W, (e.clientY - r.top) / r.height * VIEW_H), gx = Math.round(u.gx), gy = Math.round(u.gy); return gx < 0 || gy < 0 || gx >= settings.grid || gy >= settings.grid ? null : { gx, gy }; }
  function onCanvas(e: React.PointerEvent<HTMLCanvasElement>) { const p = cell(e); setHover(p); if (!p) return; const k = cellKey(p.gx, p.gy); if (tool === "paint") { setTerrain(v => ({ ...v, [k]: selectedTerrain })); setStatus(`PAINTED ${activeTerrain.label} · GRID ${p.gx}, ${p.gy}`); } else if (tool === "fill") { setTerrain(allCells(settings.grid, selectedTerrain)); setStatus(`FILLED WORLD WITH ${activeTerrain.label}`); } else if (tool === "erase") { setObjects(v => v.filter(o => o.gx !== p.gx || o.gy !== p.gy)); setTerrain(v => { const n = { ...v }; delete n[k]; return n; }); setStatus(`ERASED GRID ${p.gx}, ${p.gy}`); } else if (tool === "place") { if (!selectedAsset) return setStatus("SELECT A VALIDATED OBJECT ASSET FIRST"); setObjects(v => [...v, { id: `o-${Date.now()}`, assetId: selectedAsset, gx: p.gx, gy: p.gy }]); setStatus(`PLACED OBJECT · GRID ${p.gx}, ${p.gy}`); } else setStatus(`SELECTED GRID ${p.gx}, ${p.gy}`); }

  function resetFactory() { setDrafts([]); setPick(null); setReal(null); setError(""); }
  function chooseObject(s: Spec) { setSpecKey(s.key); setPrompt(s.prompt); resetFactory(); }
  function chooseFoundation(k: string, label: string, p: string) { setFoundationKey(k); setPrompt(p); resetFactory(); }
  function blueprints() { const notes = ["clean readable silhouette and proportions", "balanced game-readable composition", "more organic pixel variation", "strongest visual readability for the platform"]; const base = factoryKind === "foundation" ? `${prompt.trim() || "A custom foundation"}. Seamless-looking isometric ground tile, no objects, no characters, no UI, full tile coverage.` : (prompt.trim() || spec.prompt); const label = factoryKind === "foundation" ? (FOUNDATION_PRESETS.find(x => x[0] === foundationKey)?.[1] || "FOUNDATION") : spec.label; setDrafts(notes.map((n, i) => ({ id: `d-${Date.now()}-${i}`, v: i + 1, label, prompt: `${base} ${n}.` }))); setPick(null); setReal(null); setError(""); }
  async function generate() { if (!draft) return; setGenerating(true); setError(""); setReal(null); try { const key = `${factoryKind}|${foundationKey}|${spec.key}|${draft.prompt}`; const saved = JSON.parse(localStorage.getItem(CK) || "{}"); if (saved[key]) { setReal(saved[key]); return; } const r = await fetch("/api/generate-asset", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ prompt: draft.prompt, kind: factoryKind }) }); const j = await r.json(); if (!r.ok) throw Error(j.error || "Generation failed"); const out = factoryKind === "foundation" ? await normalizeFoundation(`data:image/png;base64,${j.imageBase64}`) : await normalizeObject(`data:image/png;base64,${j.imageBase64}`, spec); saved[key] = out; localStorage.setItem(CK, JSON.stringify(saved)); setReal(out); } catch (e) { setError(e instanceof Error ? e.message : "Generation failed"); } finally { setGenerating(false); } }
  function accept() { if (!draft || !real) return; if (factoryKind === "foundation") { const id = `ai-foundation-${Date.now()}`; const base = FOUNDATION_PRESETS.find(x => x[0] === foundationKey)?.[1] || "CUSTOM FOUNDATION"; const t: TerrainDef = { key: id, label: `${base} AI`, color: "#5a8f3d", edge: "#9bc56e", hint: draft.prompt, image: real, ai: true }; setCustomTerrain(v => [t, ...v]); cacheTerrain(id, real); setSelectedTerrain(id); setTool("paint"); setFactory(false); setStatus(`AI FOUNDATION IMPORTED · ${t.label} · 32 × 16 TILE CONTRACT`); resetFactory(); return; } const id = `ai-${Date.now()}`; const a: Asset = { id, title: `${spec.label} VARIANT ${String(draft.v).padStart(2, "0")}`, image: real, spec }; setLibrary(v => [a, ...v]); cacheObject(id, real); setSelectedAsset(id); setFactory(false); setStatus(`IMPORTED · ${a.title} · ${a.spec.w} × ${a.spec.h}`); resetFactory(); }
  function newProject() { if (confirm("Start a completely clean project? This clears terrain, placed objects, AI object assets and AI foundation assets.")) { setObjects([]); setTerrain({}); setLibrary([]); setCustomTerrain([]); imgs.current = {}; terrainImgs.current = {}; setSelectedAsset(""); setSelectedTerrain("grass"); setTool("paint"); setStatus("READY · NEW CLEAN PROJECT · OBJECTS 0 · TERRAIN 0 · ASSETS 0"); } }
  function startMode(mode: Mode) { setSettings(v => ({ ...v, startMode: mode })); setTerrain(mode === "grass" ? allCells(settings.grid, "grass") : {}); setStatus(mode === "grass" ? "NEW GRASS WORLD READY" : mode === "empty" ? "EMPTY WORLD READY" : "TEST WORLD MODE READY"); setSettingsOpen(false); }

  return (<div style={{ minHeight: "100vh", background: "#0b1320", color: "#dce7f2", fontFamily: "monospace", padding: 10 }}>
    <header style={{ ...panel, padding: 18, display: "flex", justifyContent: "space-between", alignItems: "center", gap: 18, marginBottom: 10 }}>
      <div><div style={{ fontSize: 30, fontWeight: 900, color: "#f0c14b" }}>PIXEL<span style={{ color: "#47b9d0" }}>GAME</span> MAKER</div><div style={{ color: "#aab8c8", marginTop: 5 }}>PIXELCHAT PLATFORM V1 · PRECISION EDITOR · ASSET CONTRACT SYSTEM</div></div>
      <div style={{ display: "flex", gap: 10, flexWrap: "wrap", justifyContent: "flex-end" }}><Button onClick={newProject}>＋ NEW</Button><Button kind="nav" onClick={() => setSettingsOpen(true)}>⚙ SETTINGS</Button><Button kind="nav" onClick={() => setFactory(true)}>✦ AI FACTORY</Button><Button kind="success" onClick={() => { localStorage.setItem(PK, JSON.stringify({ objects, terrain })); setStatus("PROJECT SAVED"); }}>SAVE</Button><Button kind="danger" onClick={() => { if (confirm("Clear the current world?")) { setObjects([]); setTerrain({}); setStatus("WORLD CLEARED"); } }}>CLEAR</Button><Button kind="nav">▶ PLAY</Button></div>
    </header>
    <main style={{ display: "grid", gridTemplateColumns: "300px minmax(0, 1fr) 290px", gap: 10 }}>
      <aside style={{ ...panel, padding: 10 }}>
        <h2 style={{ ...heading, fontSize: 17 }}>FOUNDATION TERRAIN</h2><div style={{ color: "#8fa2b7", fontSize: 12, margin: "7px 0 10px" }}>PAINT THE WORLD BEFORE PLACING OBJECTS</div>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 6 }}>{allTerrain.map(t => <button key={t.key} onClick={() => { setSelectedTerrain(t.key); setTool("paint"); }} style={{ textAlign: "left", padding: 10, background: selectedTerrain === t.key ? "#2a3d50" : "#101827", color: "#e6edf5", border: `2px solid ${selectedTerrain === t.key ? "#f0c14b" : "#40546d"}`, fontFamily: "monospace", fontWeight: 900, cursor: "pointer" }}><span style={{ display: "inline-block", width: 12, height: 12, background: t.color, border: "1px solid #d9e5f0", marginRight: 7, verticalAlign: "middle" }} />{t.label}</button>)}</div>
        <div style={{ marginTop: 12, paddingTop: 12, borderTop: "1px solid #405067" }}><h2 style={{ ...heading, fontSize: 17 }}>VALIDATED OBJECT ASSETS <Button kind="nav" style={{ float: "right", minHeight: 30, padding: "4px 9px", fontSize: 12 }} onClick={() => setFactory(true)}>＋ AI</Button></h2>{library.length === 0 ? <div style={{ color: "#8fa2b7", padding: 14, border: "1px dashed #46566c", textAlign: "center", marginTop: 10 }}>NO OBJECT ASSETS YET<br />GENERATE ONLY WHAT YOU NEED</div> : library.map(a => <button key={a.id} onClick={() => { setSelectedAsset(a.id); setTool("place"); }} style={{ display: "flex", gap: 8, alignItems: "center", width: "100%", padding: 7, marginTop: 8, background: "#101827", color: "#dce7f2", border: `2px solid ${selectedAsset === a.id ? "#f0c14b" : "#42536b"}`, fontFamily: "monospace", cursor: "pointer", textAlign: "left" }}><img src={a.image} style={{ width: 42, height: 42, objectFit: "contain", imageRendering: "pixelated" }} /><span>{a.title}<small style={{ display: "block", color: "#55c4d7", marginTop: 4 }}>✓ {a.spec.w} × {a.spec.h} VALIDATED</small></span></button>)}</div>
        <div style={{ marginTop: 14, paddingTop: 12, borderTop: "1px solid #405067" }}><h2 style={{ ...heading, fontSize: 17, marginBottom: 9 }}>TOOLS</h2><div style={{ display: "grid", gap: 7 }}><Button kind="nav" active={tool === "paint"} onClick={() => setTool("paint")}>🖌 PAINT TILE</Button><Button kind="nav" active={tool === "fill"} onClick={() => setTool("fill")}>🪣 FILL WORLD</Button><Button kind="nav" active={tool === "place"} onClick={() => setTool("place")}>＋ PLACE OBJECT</Button><Button kind="nav" active={tool === "select"} onClick={() => setTool("select")}>↖ SELECT</Button><Button kind="danger" active={tool === "erase"} onClick={() => setTool("erase")}>ERASE</Button></div></div>
      </aside>
      <section style={{ ...panel, padding: 10 }}><div style={{ display: "flex", justifyContent: "space-between", color: "#aab8c8", padding: "3px 0 9px", gap: 12 }}><b>LIVE ISO WORLD · {Object.keys(terrain).length === 0 ? "EMPTY" : "TERRAIN ACTIVE"}</b><b>{tool.toUpperCase()} · {tool === "paint" || tool === "fill" ? activeTerrain.label : "OBJECT MODE"}</b></div><canvas ref={ref} width={VIEW_W} height={VIEW_H} onPointerMove={e => setHover(cell(e))} onPointerLeave={() => setHover(null)} onPointerDown={onCanvas} style={{ width: "100%", height: "auto", border: "2px solid #3b4c63", display: "block", cursor: tool === "paint" || tool === "fill" ? "crosshair" : "pointer", imageRendering: "pixelated" }} /><div style={{ color: "#aab8c8", paddingTop: 10 }}>{status}</div></section>
      <aside style={{ ...panel, padding: 14 }}><h2 style={{ ...heading, fontSize: 18 }}>PLATFORM INSPECTOR</h2><div style={{ display: "grid", gap: 11, marginTop: 15, color: "#aab8c8" }}><div><b>TILE</b><br /><span style={{ color: "#e7eef6" }}>32 × 16 PX</span></div><div><b>GRID</b><br /><span style={{ color: "#e7eef6" }}>{settings.grid} × {settings.grid}</span></div><div><b>WORLD FOUNDATION</b><br /><span style={{ color: Object.keys(terrain).length ? "#56c7b7" : "#aab8c8" }}>{Object.keys(terrain).length ? "ACTIVE" : "EMPTY"}</span></div><div><b>RENDERING</b><br /><span style={{ color: "#e7eef6" }}>PIXEL PERFECT</span></div><div><b>SMOOTHING</b><br /><span style={{ color: "#e7eef6" }}>OFF</span></div><hr style={{ width: "100%", borderColor: "#405067" }} /><div><b>OBJECTS</b><br /><span style={{ color: "#e7eef6" }}>{objects.length}</span></div><div><b>AI LIBRARY</b><br /><span style={{ color: "#e7eef6" }}>{library.length}</span></div><div><b>TERRAIN TILES</b><br /><span style={{ color: "#e7eef6" }}>{Object.keys(terrain).length}</span></div></div></aside>
    </main>
    {settingsOpen && <div style={{ position: "fixed", inset: 0, zIndex: 40, background: "rgba(4,8,14,.82)", display: "grid", placeItems: "center", padding: 16 }}><div style={{ ...panel, width: "min(760px,100%)", padding: 22 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 12 }}><h2 style={heading}>PLATFORM SETTINGS</h2><Button kind="nav" onClick={() => setSettingsOpen(false)}>CLOSE</Button></div><p style={{ color: "#aab8c8" }}>Choose how a new world should start.</p><div style={{ display: "grid", gridTemplateColumns: "repeat(3,1fr)", gap: 10 }}><Button kind="nav" active={settings.startMode === "empty"} onClick={() => startMode("empty")}>EMPTY WORLD<br /><small>GRID ONLY</small></Button><Button kind="nav" active={settings.startMode === "grass"} onClick={() => startMode("grass")}>GRASS WORLD<br /><small>FOUNDATION READY</small></Button><Button kind="nav" active={settings.startMode === "test"} onClick={() => startMode("test")}>TEST MAP<br /><small>DEV ONLY</small></Button></div><div style={{ marginTop: 18, display: "flex", gap: 10, alignItems: "center", flexWrap: "wrap" }}><label>GRID SIZE <input type="number" min={8} max={30} value={settings.grid} onChange={e => setSettings(v => ({ ...v, grid: Math.max(8, Math.min(30, Number(e.target.value) || 14)) }))} style={{ marginLeft: 8, width: 70, padding: 9, background: "#101827", color: "white", border: "1px solid #40546d", fontFamily: "monospace" }} /></label><label><input type="checkbox" checked={settings.snap} onChange={e => setSettings(v => ({ ...v, snap: e.target.checked }))} /> GRID SNAP</label><label><input type="checkbox" checked={settings.smoothing} onChange={e => setSettings(v => ({ ...v, smoothing: e.target.checked }))} /> IMAGE SMOOTHING</label></div></div></div>}
    {factory && <div style={{ position: "fixed", inset: 0, zIndex: 50, background: "rgba(4,8,14,.92)", overflow: "auto", padding: 18 }}><div style={{ ...panel, maxWidth: 1320, margin: "0 auto", padding: 18 }}><div style={{ display: "flex", justifyContent: "space-between", gap: 12, alignItems: "center" }}><div><h1 style={{ ...heading, fontSize: 27 }}>AI ASSET FACTORY</h1><div style={{ color: "#aab8c8" }}>GENERATE ONLY WHAT YOU NEED · PLATFORM RULES ARE AUTOMATIC</div></div><Button kind="nav" onClick={() => { setFactory(false); resetFactory(); }}>CLOSE</Button></div><div style={{ display: "grid", gridTemplateColumns: "290px 1fr", gap: 14, marginTop: 18 }}><aside style={{ ...panel, padding: 12 }}><h3 style={heading}>1. WHAT ARE YOU MAKING?</h3><Button kind="nav" active={factoryKind === "foundation"} onClick={() => { setFactoryKind("foundation"); setCat("nature"); setFoundationKey("grass"); setPrompt(FOUNDATION_PRESETS[0][2]); resetFactory(); }} style={{ width: "100%", marginTop: 8 }}>🧱 FOUNDATION</Button><Button kind="nav" active={factoryKind === "object"} onClick={() => { setFactoryKind("object"); setCat("nature"); setSpecKey(FIRST.key); setPrompt(FIRST.prompt); resetFactory(); }} style={{ width: "100%", marginTop: 8 }}>🌳 OBJECT</Button>{factoryKind === "foundation" ? <div style={{ marginTop: 14 }}>{FOUNDATION_PRESETS.map(([key, label, p]) => <button key={key} onClick={() => chooseFoundation(key, label, p)} style={{ width: "100%", textAlign: "left", padding: 10, marginTop: 7, background: foundationKey === key ? "#2a3d50" : "#101827", color: "#e6edf5", border: `2px solid ${foundationKey === key ? "#f0c14b" : "#40546d"}`, fontFamily: "monospace", fontWeight: 900, cursor: "pointer" }}>{label}</button>)}</div> : <div style={{ marginTop: 14 }}><div style={{ display: "flex", gap: 7, marginBottom: 9 }}>{(["nature", "building", "furniture", "character", "effect"] as const).map(c => <Button key={c} kind="nav" active={cat === c} onClick={() => { setCat(c); setSearch(""); const first = SPECS.find(s => s.cat === c) || FIRST; chooseObject(first); }}>{c.toUpperCase()}</Button>)}</div><input value={search} onChange={e => setSearch(e.target.value)} placeholder="SEARCH..." style={{ width: "100%", boxSizing: "border-box", marginTop: 4, padding: 11, background: "#101827", color: "white", border: "1px solid #40546d", fontFamily: "monospace" }} />{visible.map(s => <button key={s.key} onClick={() => chooseObject(s)} style={{ width: "100%", textAlign: "left", padding: 10, marginTop: 7, background: specKey === s.key ? "#2a3d50" : "#101827", color: "#e6edf5", border: `2px solid ${specKey === s.key ? "#f0c14b" : "#40546d"}`, fontFamily: "monospace", fontWeight: 900, cursor: "pointer" }}>{s.label}</button>)}</div>}</aside><section><h3 style={heading}>2. DESCRIBE IT</h3><textarea value={prompt} onChange={e => { setPrompt(e.target.value); resetFactory(); }} rows={6} style={{ width: "100%", boxSizing: "border-box", marginTop: 8, padding: 13, background: "#101827", color: "#eef5fb", border: "2px solid #40546d", fontFamily: "monospace", fontSize: 14 }} /><div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10, marginTop: 10 }}><div style={{ ...panel, padding: 12 }}><b style={heading}>AUTOMATIC CONTRACT</b><br /><span style={{ color: "#aab8c8" }}>{factoryKind === "foundation" ? "FOUNDATION · 32 × 16 PX · FULL TILE · NO OBJECTS" : `${spec.label} · ${spec.w} × ${spec.h} PX · TRANSPARENT · AUTO ANCHOR`}</span></div><div style={{ ...panel, padding: 12 }}><b style={heading}>CREDIT PROTECTION</b><br /><span style={{ color: "#aab8c8" }}>4 BLUEPRINTS = FREE<br />ONLY THE SELECTED IMAGE USES GENERATION<br />CACHE REUSE WHEN POSSIBLE</span></div></div><Button onClick={blueprints} style={{ width: "100%", marginTop: 10 }}>GENERATE 4 BLUEPRINTS</Button></section></div>{drafts.length > 0 && <section style={{ marginTop: 18, borderTop: "2px solid #405067", paddingTop: 16 }}><div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 10 }}><div><h2 style={heading}>GENERATED BLUEPRINTS</h2><div style={{ color: "#aab8c8" }}>CHOOSE ONE BEFORE A REAL IMAGE IS GENERATED</div></div><Button kind="success" disabled={!draft} onClick={generate}>{generating ? "GENERATING..." : "GENERATE SELECTED IMAGE"}</Button></div><div style={{ display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 10, marginTop: 12 }}>{drafts.map(d => <button key={d.id} onClick={() => { setPick(d.id); setReal(null); setError(""); }} style={{ minHeight: 150, padding: 12, background: pick === d.id ? "#22384b" : "#101827", color: "#dce7f2", border: `2px solid ${pick === d.id ? "#f0c14b" : "#40546d"}`, fontFamily: "monospace", cursor: "pointer", textAlign: "left" }}><div style={{ fontSize: 18, color: "#f0c14b", marginBottom: 8 }}>{d.label} {String(d.v).padStart(2, "0")}</div><div style={{ color: "#aab8c8", fontSize: 12 }}>{d.prompt}</div><div style={{ color: "#56c7b7", marginTop: 10 }}>✓ FREE PREVIEW</div></button>)}</div>{generating && <div style={{ marginTop: 14, padding: 14, border: "2px solid #f0c14b", color: "#f0c14b" }}>GENERATING REAL IMAGE…</div>}{error && <div style={{ marginTop: 14, padding: 14, border: "2px solid #d26b5f", color: "#ffb3a8" }}>{error}</div>}{real && <div style={{ marginTop: 14, padding: 14, border: "2px solid #45c7b5" }}><div style={{ color: "#45c7b5", fontWeight: 900 }}>REAL GENERATED IMAGE</div><img src={real} alt="generated asset" style={{ display: "block", width: 320, height: 220, objectFit: "contain", imageRendering: "pixelated", background: "#101827", marginTop: 8 }} /><div style={{ display: "flex", gap: 8, marginTop: 10 }}><Button kind="nav" onClick={generate}>REGENERATE · 1 CALL</Button><Button kind="success" onClick={accept}>ACCEPT TO LIBRARY</Button></div></div>}</section>}</div></div>}
  </div>);
}
